include<iostream>

using namespace std;
int f(int x, int y,int k)
{
if(x<=1 || y<=1) return 0;
 if(x>y) return 1+f(f(x/y,y),y);
 return 1+f(x,f(x,y/x));
 k++;
}

int main()
{
    int n,m,c;
    cin>>n>>m;
    cout<<f(n,m,c)
    return 0;


}
